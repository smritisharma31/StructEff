# ---- StructEff: large/local artifacts that must NOT be committed ----

# MMseqs2 binary (platform-specific, install per machine)
tools/

# UniRef50 / MMseqs databases (100+ GB) — directory name as used on this machine
mmseqs_db/

# Pipeline outputs
results_test/

# Model weight caches (downloaded on first run)
*.pt
*.bin
*.safetensors

# Python
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/
*.egg-info/
.venv/
venv/
*.bak

# OS / editor
.DS_Store
*.swp
